package controllers;

public class AutomatedSpriteController extends MovingSpriteController {

    public AutomatedSpriteController(double minBoundX, double minBoundY, double maxBoundX, double maxBoundY) {
        super(minBoundX, minBoundY, maxBoundX, maxBoundY);
    }
}
